pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'cd lms-fe/lmsv1 && docker build -t fe .'
                }
            }
        stage('Run') {
            steps {
                 sh 'docker run -d --name lms-fe1 -p 80:80 fe'
            }
        }
    }
 }
